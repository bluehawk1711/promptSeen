/**
 * Re-export shared types for the admin panel.
 */
export type {
  Prompt,
  Category,
  CategoryCreateInput,
  UserProfile,
  FirebaseConfig,
  BackupData,
  BackupMetadata,
} from '@repo/shared/types';
